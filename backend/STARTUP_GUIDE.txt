"""
✅ JUSTICEAI BACKEND - QUICK START GUIDE

Your complete backend is ready! Follow these steps to run it.

1️⃣  INSTALL DEPENDENCIES
────────────────────────────────────────────────────────────────

  pip install -r requirements.txt

  This will install:
  - FastAPI & Uvicorn (web server)
  - ChromaDB (vector database)
  - sentence-transformers (embeddings)
  - langdetect (language detection)
  - deep-translator (translation)
  - google-generativeai SDK (Gemini - FREE)
  - requests (for testing)
  - python-dotenv (secrets management)


2️⃣  SET UP ENVIRONMENT
────────────────────────────────────────────────────────────────

  Copy .env.example → .env:
  
    cp .env.example .env
  
  Add your Gemini API key (FREE):
  
    GEMINI_API_KEY=AIzaSy...
  
  Get from: https://aistudio.google.com/app/apikey
  
  Free Tier: 60 requests/min, unlimited queries (perfect for hackathon!)


3️⃣  RUN THE SERVER
────────────────────────────────────────────────────────────────

  python main.py

  Expected output at startup:
  
    🚀 Starting JusticeAI backend...
    Loading 10 statute chunks...
    ✓ Loaded 10 statute chunks into ChromaDB
    ✓ RAG system initialized
    INFO:     Uvicorn running on http://0.0.0.0:8000
  
  Server ready on: http://localhost:8000


4️⃣  TEST THE ENDPOINTS
────────────────────────────────────────────────────────────────

  Option A: Run the test script
  
    python test_endpoints.py
  
    This will test all 3 endpoints with sample queries in multiple languages.
  
  Option B: Use Postman/Curl
  
    Health check:
      curl http://localhost:8000/
    
    Query endpoint:
      curl -X POST http://localhost:8000/query \
        -H "Content-Type: application/json" \
        -d '{"text": "Mera boss mujhe salary nahi diya", "pincode": "110001"}'
    
    DLSA lookup:
      curl http://localhost:8000/dlsa/110001


📁 PROJECT STRUCTURE
────────────────────────────────────────────────────────────────

Core Modules (Pipeline):
  1. language_detector.py    → Detect user's language
  2. translator.py           → Translate to English for RAG
  3. classifier.py           → Domain classification (keyword-based)
  4. rag.py                  → RAG retrieval from ChromaDB
  5. llm.py                  → Claude Haiku integration
  6. letter_generator.py     → Generate formal letters

Supporting:
  7. main.py                 → FastAPI endpoints
  8. models.py               → Pydantic schemas
  9. config.py               → Constants & keywords
  10. dlsa_db.py             → DLSA database (22 districts)

Data:
  statutes/                  → 10 placeholder statute chunks
  .env                       → Your secrets (not in git!)
  requirements.txt           → Python dependencies


✨ FEATURES
────────────────────────────────────────────────────────────────

✓ 3 Endpoints
  - POST /query              → Main legal query in any language
  - POST /generate-letter    → Generate formal letters (4 types)
  - GET /dlsa/{pincode}      → Find nearest legal aid office

✓ 5 Languages Supported
  - English, Hindi, Tamil, Telugu, Bengali

✓ 6 Legal Domains
  - Labour, Domestic Violence, Civil, Criminal, RTI, SC/ST Atrocities

✓ 4 Letter Templates
  - Labour complaint, RTI application, FIR draft, DV protection order

✓ RAG System
  - 10 statute chunks loaded on startup
  - ChromaDB in-memory vector DB
  - Cosine similarity retrieval

✓ LLM Integration
  - Google Gemini 1.5 Flash (free tier, 60 req/min)
  - Strict system prompt ensures:
    • Responses in user's language (not translated to English)
    • Only citations from provided context
    • JSON output only
    • NO COST for hackathon


🚀 DEPLOYMENT NEXT
────────────────────────────────────────────────────────────────

After testing locally:
1. Deploy to Railway.app
2. Set GEMINI_API_KEY environment variable (FREE API key)
3. Connect your frontend to these 3 endpoints


📝 NEXT STEPS
────────────────────────────────────────────────────────────────

Before Hackathon:
  ☐ Replace 10 placeholder statute chunks with real text (20-30 chunks)
  ☐ Expand DLSA database from 22 to ~50 districts
  ☐ Fine-tune domain classifier keywords if needed
  ☐ Test with real users in multiple languages
  ☐ Build frontend to consume these 3 endpoints

During Hackathon:
  ☐ Deploy to Railway
  ☐ Set up monitoring/logging
  ☐ Iterate based on user feedback


❓ TROUBLESHOOTING
────────────────────────────────────────────────────────────────

"ModuleNotFoundError: No module named 'fastapi'"
  → pip install -r requirements.txt

"Could not connect to Gemini API"
  → Check GEMINI_API_KEY in .env file
  → Verify key is valid at https://aistudio.google.com/app/apikey

"ChromaDB is empty"
  → Check that statutes/ directory exists with .txt files
  → Check server startup logs for "✓ RAG system initialized"

"Language detection returns 'tr' or incorrect language"
  → langdetect sometimes misidentifies similar languages
  → Fallback to English and let LLM detect actual language


🎯 HARDWARE INFO
────────────────────────────────────────────────────────────────

Your i5 12th gen + RTX 4050 is excellent for this:
  ✓ Sentence-transformers runs fine on CPU (~2-3 sec/query)
  ✓ ChromaDB in-memory uses <200MB
  ✓ LLM calls go to Google cloud (FREE)
  ✓ No GPU acceleration needed for this stack

Total system startup: ~30 seconds (one-time embedding of 10 chunks)
Per-query latency: ~2-3 seconds (Gemini API response time)


✅ READY TO BUILD!
────────────────────────────────────────────────────────────────

Your backend skeleton is complete. Just:
1. pip install -r requirements.txt
2. Add GEMINI_API_KEY to .env (FREE from https://aistudio.google.com/app/apikey)
3. python main.py

Questions? Check README.md for full docs.
"""

if __name__ == "__main__":
    with open("STARTUP_GUIDE.txt", "w") as f:
        f.write(__doc__)
    print(__doc__)
